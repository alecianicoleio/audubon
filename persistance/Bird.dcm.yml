Bird:
  type: entity
  id:
    id:
      type: integer
      generator:
        strategy: AUTO
  fields:
    species:
      type: string
    description:
      type: string
  oneToMany:
    sightings:
      targetEntity: Sighting
      mappedBy: Bird
